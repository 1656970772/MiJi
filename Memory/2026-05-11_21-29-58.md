# 对话记录：UEBridgeMCP 仓库资料采集

- 时间：2026-05-11T21:29:58+08:00
- 用户输入：`https://github.com/uuuuzz/UEBridgeMCP`
- 处理方式：按本项目资料库规则，使用 Codex Chrome 插件打开真实 GitHub 页面确认仓库，再用 GitHub API 核对仓库元信息、README.zh-CN、`.uplugin`、CHANGELOG、Release 与目录树。

## 本次新增

- 新增资料卡：`raw/unreal-engine/github_uuuuzz_uebridgemcp.md`
- 来源仓库：`https://github.com/uuuuzz/UEBridgeMCP`
- 仓库定位：面向 Unreal Engine 5.6+ 的原生 C++ Model Context Protocol 插件，用于让 AI 客户端通过 HTTP 与 UE Editor 交互。
- 许可证：GitHub API 与仓库 LICENSE 显示为 `GPL-3.0`。

## 核对事实

- 仓库为公开仓库，默认分支 `main`，主语言 `C++`。
- 本次截取时，仓库约 `37` stars、`7` forks、`2` open issues。
- 公开 release 为 `v1.19.0`，发布时间为 `2026-04-19 19:55:48 +08:00`。
- 目录树包含 `553` 个 blob 文件、`140` 个目录，其中 `.cpp` 文件 `215` 个、`.h` 文件 `218` 个、Markdown 文件 `89` 个。
- `.uplugin` 显示 `VersionName: 1.19.0`，`IsExperimentalVersion: true`，包含 `UEBridgeMCP`、`UEBridgeMCPEditor`、`UEBridgeMCPControlRig`、`UEBridgeMCPPCG`、`UEBridgeMCPExternalAI` 五个 Editor-only 模块。

## 我的判断

这个仓库适合加入“虚幻相关”资料库，核心价值在于它把 Unreal Editor 的查询、编辑、保存、验证等能力组织成 MCP 工具面，适合作为“AI Agent 驱动 UE 编辑器自动化”的重点参考。

实际试用前建议先放入独立 UE 5.6 C++ 测试项目，而不是直接进入生产项目。依据是仓库要求 UE 5.6+、`.uplugin` 标记 experimental，且工具面会触达蓝图、关卡、资产、Widget、PIE 和构建流程。

## 待验证边界

- 本次未克隆仓库。
- 本次未编译插件。
- 本次未在 Unreal Editor 中加载或运行 MCP endpoint。
- 本次未验证 `tools/list` 的实际运行时工具清单。
