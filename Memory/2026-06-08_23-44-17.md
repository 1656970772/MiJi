# 2026-06-08 See-through 知识库添加记录

- 用户需求：把 `https://github.com/shitagaki-lab/see-through?utm_source=chatgpt.com` 添加到本项目知识库。
- 来源核对：通过 GitHub API 读取 `shitagaki-lab/see-through` 元数据与 README；仓库描述为 `"Single-image Layer Decomposition for Anime Characters" (SIGGRAPH 2026, Conditionally Accepted)`，主语言 Python，许可证 Apache-2.0，默认分支 `main`。
- 分类判断：该项目面向动漫角色单图图层分解、2.5D 模型、PSD 导出和 Live2D 前处理，更适合归入 `raw/ai-tools`，而不是引擎教程或画风参考。
- 抓取问题：直接执行 `graphify add "https://github.com/shitagaki-lab/see-through" --dir raw\ai-tools` 失败，原因是当前系统 DNS 将 `github.com` 解析为 `127.0.0.1`，触发 Graphify 的私有/本地 IP 安全拦截。
- 回退处理：使用 GitHub API 抓取 README 与仓库元数据，手工新增资料卡 `raw/ai-tools/github_com_shitagaki-lab_see-through.md`。
- 图谱更新：由于 `graphify update raw` 只做 AST/code graph 更新，不能把文档语义卡自动并入现有手工语义图；本次按现有 `raw/graphify-out/graph.json` 结构增量补充 See-through 相关节点、边和 hyperedge，并同步更新 `graph.html`、`GRAPH_REPORT.md`、`MANUAL_ANALYSIS.md`。
- 新图谱统计：117 nodes / 189 edges / 13 communities；新增社区为 `AI Character Asset Pipeline`。
- 验证结果：`python -m json.tool raw\graphify-out\graph.json` 通过；`graphify query "See-through Live2D PSD" --graph raw\graphify-out\graph.json --budget 700` 能返回 `See-through`、`PSD Export`、`Live2D Preprocessing`、`LayerDiff 3D`、`Marigold Depth` 等节点；`graph.html` 内嵌数组为 117 nodes / 189 edges；`git diff --check` 退出码为 0，仅有 Windows 换行提示。
