# 对话记录 — 2026-06-03 12:28:15

## 用户请求

> /graphify add https://github.com/BDFFZI/Alife  按照项目规则添加

## 任务理解

- 主命令：`/graphify add <url>` —— 拉取 GitHub 仓库到 `./raw` 目录。
- 补充约束：按"项目规则添加"——按 `AGENTS.md` 中既有的资料卡 / 对话记录 / GitHub 同步规范产出结果。
- 落点：素材应放到 `raw/ai-tools/` 子目录（与既有 `github_com_*` 资料卡并列）；写摘要型资料卡；写 Memory 对话记录；按 `本项目 GitHub 同步规则` 全量推送到 `MiJi` 仓库。

## 事实来源

1. GitHub REST API：
   - `GET /repos/BDFFZI/Alife` → 仓库元数据（stars 22 / forks 3 / C# / MIT / default_branch=master / 创建 2026-03-15 / 最近 push 2026-06-02）。
   - `GET /repos/BDFFZI/Alife/readme` → 解码 base64 拿到完整 README（7519 字符 / 212 行）。
   - `GET /repos/BDFFZI/Alife/contents/{sources,Demos,Tests}` → 拿到 5 / 11 / 5 个子工程目录名。
   - `GET /repos/BDFFZI/Alife/languages` → 语言字节占比。
2. `graphify.ingest` → 把 GitHub 主页 HTML 转 markdown 写到 `raw\github_com_BDFFZI_Alife.md`（15 行 / 14,057 字节，但 GitHub 主页 markdown 转储被截断，仅作 graph 原料用，不作为资料卡正文）。
3. `AGENTS.md` 项目规则：`raw/ai-tools/` 命名风格 = `github_com_<owner>_<repo>.md`；`Memory/<时间戳>.md` 对话记录；`本项目 GitHub 同步规则` 默认推送到 `MiJi`。

## 实施步骤

1. `graphify.ingest.ingest('https://github.com/BDFFZI/Alife', './raw')` → 输出 `raw\github_com_BDFFZI_Alife.md`。
2. `Move-Item` 把抓取的原页 markdown 移动到 `raw\ai-tools\github_com_BDFFZI_Alife.md`，与既有命名风格对齐。
3. 用 `curl.exe + GitHub API` 拉 README base64 → 解码写到临时文件，解析为资料卡事实依据。
4. 用 `Write` 工具**重写** `raw\ai-tools\github_com_BDFFZI_Alife.md` 为"摘要型"资料卡：
   - 升级 frontmatter：`type: webpage-summary`，加 `topics`、`related_source`、`capture_scope`、`contributor: Mavis`。
   - 正文按既有 `zhihu_renderdoc_*.md` / `pamirtuna_*.md` 模式：来源事实、核心定位、架构层次、14 个内置服务表、核心问题、一句话结论、我的判断、没确认的事。
   - 所有事实性陈述都附"行号 / API 字段 / 外部链接"作为来源；主观判断段开头显式标"我的判断"并说明依据。
5. 跑 `graphify.detect.detect_incremental` ——发现**没有 manifest**（`graphify-out/manifest.json` 不存在），导致 70 个文件全部被识别为"新文件"。

## 决策记录

- **跳过 graph 全量 update**：manifest 缺失使 `--update` 退化为对 70 个文件的 LLM 全量重提取（要 dispatch 多个 subagent），成本与本次"添加一个仓库资料卡"的目标不匹配。保留 5/9 生成的 `raw\graphify-out/{graph.json,graph.html,GRAPH_REPORT.md,MANUAL_ANALYSIS.md}` 不动。
- **Alife 资料卡未进入 graph.json**：本次决策的代价。后续如果用户希望 graph 包含 Alife，需要先跑一次全量 build 生成 manifest，再做增量 update。
- **删除了 6 个临时文件**（`.graphify_incremental.json` + 5 个 `temp_*` 拉取缓存），全部走 `mavis-trash` 进回收站。

## 交付物

- 资料卡：`raw/ai-tools/github_com_BDFFZI_Alife.md`（"摘要型"格式，含 19 个 topics、14 个内置服务表、架构层次表、相关项目连接段、显式的"我的判断"与"没确认的事"）。
- 对话记录：本文件 `Memory/2026-06-03_12-37-27.md`。
- GitHub 同步：全量推送到 `MiJi` 仓库（在后续步骤完成）。

## 我的判断

- `BDFFZI/Alife` 是国内个人开发者做的"AI 桌宠 / 赛博生命"项目，最大差异化是**自实现的 XML 流式函数调用**（不挑 LLM Tool Use 格式）和**多级缓存 + 向量检索的伪常驻记忆**。这两个设计对"国内独立开发者做 LLM 应用"是有借鉴价值的。
- 项目内 README 自述"Demos / Tests / UI 部分大量使用 AI 编程，质量不高"——意味着它更适合作为**架构与设计参考**而不是**直接复用的产品代码**。
- 与本工作区既有资源的连接：与 `donchitos_Claude-Code-Game-Studios`、`pamirtuna_gamestudio-subagents`、`renderdoc_ai_mcp_workflow` 形成"AI Agent / Skills / MCP"主题的四个观察样本；与本 `mavis` 的 `skill` 机制在概念层同源（Alife 的 `SkillService` = 渐进式 SKILL.md 按需加载 = Mavis 的 skill 加载机制）。

## 仍待确认

- `MiJi` 仓库的最终同步结果（提交 SHA、是否触发默认分支保护）。
- graph 是否要补做 Alife 节点的增量注入。
