# 对话记录：mosaic_game 仓库资料采集

- 时间：2026-05-11T21:34:52+08:00
- 用户输入：`https://github.com/huangfeidian/mosaic_game`
- 处理方式：使用 Codex Chrome 插件打开真实 GitHub 页面确认仓库，再用 GitHub API 核对仓库元信息、README、CMake、目录树、关键目录与 release。

## 本次新增

- 新增资料卡：`raw/game-design/github_huangfeidian_mosaic_game.md`
- 来源仓库：`https://github.com/huangfeidian/mosaic_game`
- 仓库定位：基于 C++17 的分布式大世界游戏服务端示例项目，支持无缝迁移，并包含服务端角色、客户端角色、代码生成、数据导出、MongoDB / Redis 配置和 Linux 批量启动脚本。

## 核对事实

- 仓库为公开仓库，默认分支 `master`，主语言 `C++`。
- GitHub API 未识别许可证，本次也未在根目录列表中看到 `LICENSE` 文件。
- 本次截取时，仓库约 `190` stars、`49` forks、`0` open issues。
- GitHub Release API 返回空列表。
- 目录树包含 `708` 个 blob 文件、`204` 个目录，其中 `.cpp` 文件 `223` 个、`.h/.hpp` 文件 `279` 个、Python 文件 `12` 个。
- 服务端角色目录包含 `mgr_server`、`gate_server`、`service_server`、`space_server`、`map_server`、`db_server`、`redis_server`。

## 我的判断

这个仓库适合归入“游戏服务端 / 游戏架构”资料，而不是“虚幻相关”。依据是 README、topics 和目录结构都指向 C++ 游戏服务器、分布式空间、MongoDB、Redis、服务拓扑和代码生成，没有 Unreal Engine 插件或项目结构。

后续若要深入研究，建议按 `Readme.md` -> `roles/server/` -> `common/` -> `src/network` / `src/stub` -> `generator/script` -> `data/config` 的顺序阅读。依据是 README 的构建与运行流程显示，服务角色和代码生成链路是理解该仓库的主干。

## 待验证边界

- 本次未克隆仓库。
- 本次未安装依赖。
- 本次未生成 RPC / property 代码。
- 本次未编译或运行任何服务进程。
