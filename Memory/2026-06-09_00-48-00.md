# 2026-06-09 对话记录：添加 PlotPilot 到知识库

## 用户需求

- 用户要求将 GitHub 项目 `shenminglinyi/PlotPilot` 添加到当前 MiJi 项目知识库。

## 执行过程

- 使用 GitHub API 读取仓库元数据与 README。
- 由于本机 DNS 将 `github.com` 解析到 `127.0.0.1`，`graphify add` 的远程安全校验路径不可用，因此采用 GitHub API 读取资料、手动沉淀资料卡并增量更新 Graphify 图谱。
- 新增资料卡：`raw/ai-tools/github_com_shenminglinyi_PlotPilot.md`。
- 更新 Graphify 输出：
  - `raw/graphify-out/graph.json`
  - `raw/graphify-out/graph.html`
  - `raw/graphify-out/GRAPH_REPORT.md`
  - `raw/graphify-out/MANUAL_ANALYSIS.md`

## 资料摘要

- 来源仓库：`https://github.com/shenminglinyi/PlotPilot`
- 项目定位：PlotPilot（墨枢）是面向长篇 AI 创作的开源剧情引擎内核。
- 核心方向：持久记忆、知识图谱、自动推进流水线、质量治理闭环。
- 技术栈线索：Python、FastAPI、Vue 3、TypeScript、SQLite、ChromaDB/FAISS、OpenAI 兼容模型等。
- 我的判断：该项目适合作为 MiJi 知识库中的“AI 创作基础设施 / 长篇叙事工程 / 写作自动化引擎”资料，并与 `Game Narrative Text`、`AI Game DevTools`、`Game Studio Sub-Agents` 等节点建立推断关系。

## 图谱更新结果

- Graphify 图谱更新后统计：`137 nodes / 213 edges / 14 communities`。
- 新增社区：`AI Narrative Engine`。
- 新增核心节点包括：
  - `PlotPilot（墨枢）`
  - `Narrative Engine Kernel`
  - `Long-form AI Creation`
  - `Narrative State Machine`
  - `Story Bible`
  - `Chapter Summary Chain`
  - `Narrative Event Stream`
  - `Storyline DAG`
  - `Foreshadowing Registry`
  - `Vector Retrieval Layer`
  - `Triple Index`
  - `EngineDaemon`
  - `StoryPipelineRunner`
  - `BaseStoryPipeline`
  - `Prompt Strategy Layer`
  - `Quality Monitor`
  - `Tension ECG`
  - `Write Dispatch`
  - `FastAPI REST API`
  - `Vue Workbench`

## 验证

- 已通过 `python -m json.tool raw\graphify-out\graph.json` 校验 JSON。
- 已通过 `graphify query "PlotPilot Narrative State Machine Game Narrative Text" --graph raw\graphify-out\graph.json --budget 800` 查询到 PlotPilot 及相关叙事引擎、游戏叙事节点。
- 已使用 `rg` 确认 PlotPilot 已写入资料卡、Graphify JSON、HTML、报告与手工分析文件。
- `git diff --check` 仅出现 CRLF/LF 环境提示，命令退出码为 0，无真实 whitespace error。

## 阻塞与临时处理

- 阻塞点：本机 DNS 将 `github.com` 解析为 `127.0.0.1`，会拖慢依赖远程 URL 校验的 Graphify 自动导入流程。
- 临时处理：本次绕过 `graphify add`，改用 GitHub API 获取资料并手动增量维护图谱。
- 建议：后续可由用户决定是否将“GitHub DNS 异常时使用 GitHub API 回退”沉淀为项目规则或全局规则。
