# 2026-05-11 19:55:37 对话记录

## 本次输入

用户提供知乎链接：

- `https://zhuanlan.zhihu.com/p/1989826376426078860`

上下文偏好：

- 用户希望后续打开知乎等页面时优先使用真实浏览器方式，最好使用 Codex Chrome 插件。
- 当前项目要求默认中文沟通与中文资料整理。
- 当前项目要求事实、判断和建议标注来源；分析性内容需写明“我的判断”或“我的观点”。
- 当前项目任务完成后默认同步 GitHub 仓库 `MiJi`，并优先使用 GitHub API。

## 浏览与提取

使用 Codex Chrome 插件读取知乎页面。页面信息如下：

- 标题：`TA实践分享：简单且只采样一次的三面映射(Unity+UE）`
- 作者：`彦青`
- 作者简介片段：`北京电影学院 艺术设计硕士`
- 页面底部显示：`编辑于 2025-12-31 23:22・浙江`
- 页面标签：`TA技术美术`、`UE引擎`
- 正文长度约 4315 字。

文章主要内容：

- 传统三面映射需要三次纹理采样，并进行两次 `lerp` 混合。
- 本文方案通过主导轴选择实现一次纹理采样。
- 方案支持三种 UV 模式：`World Position UV`、`Object Position UV`、`Mesh UV0（支持模型缩放调整）`。
- 算法先取法线在 X/Y/Z 上的绝对值，再生成 one-hot 主导面遮罩。
- 文章提醒遮罩计算需要在片元着色器中完成，避免顶点插值导致遮罩变成混合权重。
- Unity 代码核心使用 HLSL `step` 生成 `face.x/y/z`，再根据遮罩组合 `finalUV`，最后单次 `tex2D(_MainTex, finalUV)`。
- UE 部分以材质节点图示为主，包括材质节点总览、遮罩计算、UV 计算与采样。

## 外部来源核验

补充核验来源：

- Microsoft Learn HLSL `step` 文档说明 `step(y, x)` 返回值等价于 `(x >= y) ? 1 : 0`。
- Unreal Engine 官方 Texturing 文档说明 `WorldAlignedTexture` 可在世界空间平铺纹理，`WorldCoordinate3Way` 会把纹理投影到 XY、YZ、XZ 平面并控制边缘混合。
- Unity 官方手册说明 ShaderLab 2D texture 属性在 Cg/HLSL 中映射为 `sampler2D`，并会提供 `_ST` tiling/offset 参数。

## 产出文件

创建资料卡：

- `raw/unreal-engine/zhihu_zhuanlan_1989826376426078860_single_sample_triplanar_mapping_unity_ue.md`

资料卡重点：

- 把文章方案归纳为“主导轴硬选择版 triplanar/cube-projection 简化”。
- 区分传统三面映射的三次采样软混合与本文方案的一次采样硬切换。
- 说明 Unity HLSL 实现中的 one-hot mask、片元阶段计算、Mesh UV0 缩放补偿。
- 说明 UE 落地可用材质节点复刻：法线绝对值、主导轴遮罩、UV 选择、单个 Texture Sample。
- 标注适合自然材质、程序化网格、性能敏感平台；不适合强方向纹理、近景高质量材质和需要软过渡的英雄资产。

## 我的判断

这篇资料最适合归档到 `虚幻 / 材质 / 三面映射 / WorldAlignedTexture / Shader性能优化`，同时保留 `Unity HLSL` 标签。

核心价值是把传统三面映射的三次采样换成一次采样，但代价是主导轴硬切换带来的潜在断裂感。它适合作为性能模式或工具箱中的可选材质函数，而不是无条件替代传统 triplanar mapping。

依据：知乎原文明确描述 3 次采样到 1 次采样的优化；Microsoft HLSL 文档验证 `step` 的比较语义；Unreal 官方文档说明三向世界坐标投影与边缘混合属于材质函数中的常见能力。

## 来源

- 用户提供知乎链接：`https://zhuanlan.zhihu.com/p/1989826376426078860`
- Microsoft Learn HLSL `step`：`https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-step`
- Unreal Engine Texturing 文档：`https://dev.epicgames.com/documentation/en-us/unreal-engine/texturing?application_version=4.27`
- Unity 手册：`https://docs.unity3d.com/2019.4/Documentation/Manual/SL-PropertiesInPrograms.html`
