# 对话记录

- 时间：2026-05-04 14:29:56（Asia/Shanghai）
- 用户需求：添加 Bilibili 视频 `https://www.bilibili.com/video/BV1VKRPBTE6H/` 到当前资料项目。
- 来源：用户提供链接；Bilibili 元数据由本地 `yt-dlp 2026.03.17` 解析。
- 视频标题：`5分钟完全了解Unity-运动插值`
- 视频作者：`数据传输小助手`
- 我的判断：该视频应归入 `raw/unity-engine`；依据是标题包含 Unity，标签包含 `UNITY开发教程`、`运动插值`、`横版相机控制`、`角色3C`，视频简介包含 Unity 动作游戏项目仓库链接。
- 执行结果：已新增 `raw/unity-engine/bilibili_BV1VKRPBTE6H_Unity_motion_interpolation.md`，删除 `graphify add` 生成的乱码网页抓取文件，并准备刷新 `raw/graphify-out` 图谱产物。
- 初始限制说明：第一次添加时未生成视频语音转写；后续复核发现 `graphify.transcribe` 依赖的 `faster-whisper` 与 Python 版 `yt-dlp` 已可用，并已在 14:42 重跑处理中补齐转写。

## 2026-05-04 14:42 重跑 graphify add

- 用户请求：重新执行 `graphify add` 添加同一个 Bilibili 链接。
- 执行结果：`graphify add` 成功保存 `raw/unity-engine/www_bilibili_com_video_BV1VKRPBTE6H.md`，但文件正文仍是 B 站压缩响应乱码，因此已删除该文件，避免污染语料。
- 依赖复核：`graphify.transcribe` 实际依赖 `faster-whisper` 和 Python 版 `yt-dlp`；这两个依赖本机已可用。此前检查 `openai-whisper` 包名导致判断偏粗。
- 转写结果：已调用 `graphify.transcribe` 下载音频并转写，生成 `raw/unity-engine/transcripts/yt_30c3774e422c.txt`，音频缓存为 `raw/unity-engine/transcripts/downloads/yt_30c3774e422c.m4a`。
- 后续处理：更新视频资料卡，并把 Unity 运动插值相关节点补入 `raw/graphify-out`。
